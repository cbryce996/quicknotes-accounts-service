const makeUser = require('../makeUserService');

// Operation being tested
describe('creating a user object', () => {
    // What condition is being tested
    describe('given an empty username', () => {
        // Result being tested
        it('should throw an error', () => {
        });
    })
})